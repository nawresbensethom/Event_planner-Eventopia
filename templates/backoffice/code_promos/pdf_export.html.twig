<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Liste des codes promotionnels</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            color: #333;
            line-height: 1.4;
        }
        .container {
            width: 100%;
            margin: 0 auto;
        }
        .header {
            text-align: center;
            margin-bottom: 30px;
            padding-bottom: 10px;
            border-bottom: 2px solid #5e72e4;
        }
        .header h1 {
            color: #5e72e4;
            margin-bottom: 5px;
        }
        .header p {
            color: #777;
            font-size: 14px;
            margin-top: 0;
        }
        table {
            width: 100%;
            border-collapse: collapse;
            margin-bottom: 20px;
        }
        table, th, td {
            border: 1px solid #ddd;
        }
        th {
            background-color: #5e72e4;
            color: white;
            padding: 10px;
            text-align: left;
        }
        td {
            padding: 8px 10px;
        }
        tr:nth-child(even) {
            background-color: #f2f2f2;
        }
        .status-active {
            color: #2dce89;
            font-weight: bold;
        }
        .status-expired {
            color: #f5365c;
            font-weight: bold;
        }
        .footer {
            text-align: center;
            font-size: 12px;
            color: #777;
            margin-top: 30px;
            border-top: 1px solid #ddd;
            padding-top: 10px;
        }
        .footer p {
            margin: 5px 0;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h1>Liste des codes promotionnels</h1>
            <p>Généré le {{ "now"|date("d/m/Y à H:i") }}</p>
        </div>
        
        <table>
            <thead>
                <tr>
                    <th>Code</th>
                    <th>Type de réduction</th>
                    <th>Début</th>
                    <th>Expiration</th>
                    <th>Statut</th>
                </tr>
            </thead>
            <tbody>
                {% for code in code_promos %}
                    <tr>
                        <td>{{ code.code }}</td>
                        <td>{{ code.reductionType }}</td>
                        <td>{{ code.dateDebut|date('d/m/Y') }}</td>
                        <td>{{ code.dateExpiration|date('d/m/Y') }}</td>
                        <td>
                            {% if code.dateExpiration > date() %}
                                <span class="status-active">Actif</span>
                            {% else %}
                                <span class="status-expired">Expiré</span>
                            {% endif %}
                        </td>
                    </tr>
                {% endfor %}
            </tbody>
        </table>
        
        <div class="footer">
            <p>EvenTopia - Système de gestion des codes promotionnels</p>
            <p>Document confidentiel - Usage interne uniquement</p>
        </div>
    </div>
</body>
</html> 