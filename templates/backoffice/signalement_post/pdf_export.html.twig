<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Détails du Signalement</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            color: #333;
            line-height: 1.4;
        }
        .container {
            width: 100%;
            margin: 0 auto;
        }
        .header {
            text-align: center;
            margin-bottom: 30px;
            padding-bottom: 10px;
            border-bottom: 2px solid #5e72e4;
        }
        .header h1 {
            color: #5e72e4;
            margin-bottom: 5px;
        }
        .header p {
            color: #777;
            font-size: 14px;
            margin-top: 0;
        }
        .info-section {
            margin-bottom: 25px;
            padding: 15px;
            background-color: #f8f9fe;
            border-radius: 5px;
        }
        .info-title {
            font-weight: bold;
            color: #5e72e4;
            margin-bottom: 10px;
            border-bottom: 1px solid #ddd;
            padding-bottom: 5px;
        }
        .content-box {
            padding: 15px;
            border: 1px solid #ddd;
            border-radius: 5px;
            background-color: #fff;
            margin-bottom: 20px;
        }
        .status {
            display: inline-block;
            padding: 5px 10px;
            border-radius: 15px;
            font-weight: bold;
            font-size: 14px;
        }
        .status-en-attente {
            background-color: #fff3cd;
            color: #856404;
        }
        .status-traite {
            background-color: #d4edda;
            color: #155724;
        }
        .footer {
            text-align: center;
            font-size: 12px;
            color: #777;
            margin-top: 30px;
            border-top: 1px solid #ddd;
            padding-top: 10px;
        }
        .footer p {
            margin: 5px 0;
        }
        table {
            width: 100%;
            border-collapse: collapse;
        }
        table td {
            padding: 8px;
        }
        .label {
            font-weight: bold;
            width: 150px;
            color: #555;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h1>Rapport de Signalement</h1>
            <p>Généré le {{ "now"|date("d/m/Y à H:i") }}</p>
        </div>
        
        <div class="info-section">
            <div class="info-title">Informations Générales</div>
            <table>
                <tr>
                    <td class="label">Statut:</td>
                    <td>
                        <span class="status status-{{ signalement_post.statut|lower|replace({' ': '-'}) }}">
                            {{ signalement_post.statut }}
                        </span>
                    </td>
                </tr>
                <tr>
                    <td class="label">ID du signalement:</td>
                    <td>{{ signalement_post.idSignalementPost }}</td>
                </tr>
                <tr>
                    <td class="label">Date:</td>
                    <td>{{ signalement_post.dateSignalement|date('d/m/Y à H:i') }}</td>
                </tr>
                <tr>
                    <td class="label">Signalé par:</td>
                    <td>{{ signalement_post.idUtilisateur.nom }}</td>
                </tr>
            </table>
        </div>
        
        <div class="info-section">
            <div class="info-title">Publication Signalée</div>
            <table>
                <tr>
                    <td class="label">Titre du post:</td>
                    <td>{{ signalement_post.idPost.titre }}</td>
                </tr>
                <tr>
                    <td class="label">ID du post:</td>
                    <td>{{ signalement_post.idPost.idPost }}</td>
                </tr>
                <tr>
                    <td class="label">Auteur:</td>
                    <td>{{ signalement_post.idPost.idUtilisateur.nom }}</td>
                </tr>
                <tr>
                    <td class="label">Date publication:</td>
                    <td>{{ signalement_post.idPost.datePublication|date('d/m/Y à H:i') }}</td>
                </tr>
            </table>
        </div>
        
        <div class="content-box">
            <div class="info-title">Motif du signalement</div>
            <p>{{ signalement_post.raison }}</p>
        </div>
        
        <div class="content-box">
            <div class="info-title">Contenu de la publication signalée</div>
            <p>{{ signalement_post.idPost.contenu }}</p>
        </div>
        
        <div class="footer">
            <p>EvenTopia - Système de gestion des signalements</p>
            <p>Document confidentiel - Usage interne uniquement</p>
        </div>
    </div>
</body>
</html> 