<form method="post" action="{{ path('client_offreemploi_delete', {'id_offre': offreemploi.idOffre}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ offreemploi.idOffre) }}">
    <button class="btn">Delete</button>
</form>